const crypto = require('crypto-js')
    , session = require('express-session')

module.export = (from, login, pw) => {

    switch (from) {

        case 'admin':
            break
        case 'fornecedor':
            break
        case 'comprador':
            break
        default:

    }

}
